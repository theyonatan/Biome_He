[project]
name = "biome-server"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
  "world-engine @ https://github.com/Overworldai/world_engine/archive/refs/heads/wp-1.5.zip",
  "torch",
  "torchvision",
  "pillow",
  "fastapi>=0.128.0",
  "uvicorn>=0.40.0",
  "httpx>=0.27.0",
  "websockets>=15.0.1",
  "hf-xet>=1.0.0",
  "transformers>=4.30.0",
  "timm>=0.9.0",
]

[dependency-groups]
dev = []

[tool.uv]
index-strategy = "unsafe-best-match"

[tool.uv.sources]
torch = { index = "pytorch-cu128" }
torchvision = { index = "pytorch-cu128" }

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true
